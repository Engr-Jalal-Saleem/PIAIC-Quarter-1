"use strict";
/* The code is creating an array called `guest_name` which contains the names of the guests invited to
dinner. It then prints the original guest list using `console.log("Original guest list:",
guest_name)`. Finally, it prints the number of people invited to dinner using `console.log("Number
of people invited to dinner:", guest_name.length)`. The `length` property of an array returns the
number of elements in the array. */
// Dinner Guests: Working with one of the programs from Exercises 14 through 18, print a message indicating the number of people you are inviting to dinner.
let guest_name = [
    "Adeel",
    "Afzaal",
    "Umer",
    "Qasim",
    "ali",
    "Kashif",
    "Ahsan",
    "Azeem",
];
console.log("Original guest list:", guest_name);
console.log("Number of people invited to dinner:", guest_name.length);
